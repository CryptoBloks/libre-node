# Libre Node Docker Compose Configuration
# Generated by libre-node setup wizard
# Template version: 3.0

services:
  {{CONTAINER_NAME}}:
    image: {{IMAGE_NAME}}
    container_name: {{CONTAINER_NAME}}
    command: >
      {{NODEOS_COMMAND}}
    volumes:
      - {{CONFIG_DIR}}:/opt/eosio/config
      - {{DATA_DIR}}:/opt/eosio/data
      - {{LOGS_DIR}}:/opt/eosio/logs
      - {{SNAPSHOTS_DIR}}:/opt/eosio/data/snapshots
{{TMPFS_VOLUMES}}
{{EXTRA_VOLUMES}}
    network_mode: host
    restart: {{RESTART_POLICY}}
    stop_grace_period: {{STOP_GRACE_PERIOD}}
{{HEALTHCHECK}}
{{ENVIRONMENT}}

{{GATEWAY_SERVICE}}
